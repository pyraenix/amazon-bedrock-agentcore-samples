# AWS Configuration
# Set your AWS region (default: us-east-1)
AWS_REGION=us-east-1

# AWS Credentials (optional - can use AWS CLI or IAM roles instead)
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Optional: AWS Profile (if using AWS CLI profiles)
# AWS_PROFILE=your_profile_name

# Optional: Custom S3 bucket for recordings
# S3_RECORDING_BUCKET=your-recordings-bucket
# S3_RECORDING_PREFIX=sessions/

# Optional: IAM role for browser execution
# RECORDING_ROLE_ARN=arn:aws:iam::YOUR_ACCOUNT:role/BedrockAgentCoreRole